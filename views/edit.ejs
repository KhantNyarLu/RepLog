<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>RepLog – Edit Workout</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="app-shell">

      <header class="app-header">
        <div class="app-header-inner">
          <div class="logo-mark">
            <span>🏋</span>
          </div>
          <div class="logo-text">
            <h1>RepLog</h1>
            <p>Track your progress one rep at a time.</p>
          </div>

          
          <div class="header-actions">
            <a href="/entries/new" class="btn btn-primary">+ Log Workout</a>
          </div>
        </div>
      </header>

    
      <main class="page-main">
     
        <section class="page-header">
          <a href="/" class="back-link">← Back to Dashboard</a>
          <h1>Edit Workout</h1>
          <p>Update or delete this workout entry.</p>
        </section>

        
        <form
          action="/entries/<%= entry._id %>?_method=PUT"
          method="POST"
          class="card form-card"
        >
         
          <div class="form-grid">
            <div class="form-group">
              <label for="date">Date</label>
              <input
                id="date"
                name="date"
                type="date"
                value="<%= entry.date.toISOString().slice(0, 10) %>"
                required
              />
            </div>

            <div class="form-group">
              <label for="exercise">Exercise</label>
              <input
                id="exercise"
                name="exercise"
                type="text"
                placeholder="e.g., Barbell Bench Press"
                value="<%= entry.exercise %>"
                required
              />
            </div>
          </div>

          
          <div class="form-group">
            <label for="muscleGroup">Muscle Group</label>
            <select id="muscleGroup" name="muscleGroup" required>
              <option value="">Select muscle group</option>
              <option value="Chest"     <%= entry.muscleGroup === 'Chest' ? 'selected' : '' %>>Chest</option>
              <option value="Back"      <%= entry.muscleGroup === 'Back' ? 'selected' : '' %>>Back</option>
              <option value="Legs"      <%= entry.muscleGroup === 'Legs' ? 'selected' : '' %>>Legs</option>
              <option value="Shoulders" <%= entry.muscleGroup === 'Shoulders' ? 'selected' : '' %>>Shoulders</option>
              <option value="Arms"      <%= entry.muscleGroup === 'Arms' ? 'selected' : '' %>>Arms</option>
              <option value="Full Body" <%= entry.muscleGroup === 'Full Body' ? 'selected' : '' %>>Full Body</option>
            </select>
          </div>

          
          <div class="form-grid four-cols">
            <div class="form-group">
              <label for="sets">Sets</label>
              <input
                id="sets"
                name="sets"
                type="number"
                min="1"
                value="<%= entry.sets %>"
                required
              />
            </div>

            <div class="form-group">
              <label for="reps">Reps (e.g., 12-12-10)</label>
              <input
                id="reps"
                name="reps"
                type="text"
                placeholder="e.g., 8-8-6"
                value="<%= entry.reps %>"
                required
              />
            </div>

            <div class="form-group">
              <label for="weight">Weight (lbs)</label>
              <input
                id="weight"
                name="weight"
                type="number"
                step="0.1"
                min="0"
                value="<%= entry.weight || '' %>"
                required
              />
            </div>

            <div class="form-group">
              <label for="rpe">RPE (1–10)</label>
              <input
                id="rpe"
                name="rpe"
                type="number"
                min="1"
                max="10"
                value="<%= entry.rpe || '' %>"
                required
              />
            </div>
          </div>

          
          <div class="form-group">
            <label for="notes">Notes</label>
            <textarea
              id="notes"
              name="notes"
              rows="4"
              placeholder="How did the set feel? Any observations..."
            ><%= entry.notes || '' %></textarea>
          </div>

          
          <div class="form-actions">
            <a href="/" class="btn btn-ghost">Cancel</a>
            <button type="submit" class="btn btn-primary">Save Changes</button>
          </div>
        </form>

        
        <form
          action="/entries/<%= entry._id %>?_method=DELETE"
          method="POST"
          class="delete-form"
        >
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </main>
    </div>
  </body>
</html>
